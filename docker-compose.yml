version: '3'
services:

  ai_flask:
    container_name: ai_flask
    build:
      context: ./
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    entrypoint:
      - sh
      - config/docker/entrypoint.sh